# Railway.app deployment config (config-as-code)
# https://docs.railway.app/guides/config-as-code

[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node dist/index.cjs"
healthcheckPath = "/api/infra/health"
healthcheckTimeout = 30
